// src/app/(dashboard)/layout.jsx
// Route group layout — shared by /dashboard, /analytics, /signals
// (dashboard) folder = groups routes WITHOUT changing the URL

export default function DashboardGroupLayout({ children }) {
  return <>{children}</>
}
